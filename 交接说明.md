# 红小涛学习套装 · 交接说明书（2026-08-08 版，一份全包）

> **给新对话的 AI 的第一句话**：把这份文件从头读到尾，读完后用三句大白话告诉洪老师你明白了什么（现在几个软件、都是什么版本、最要紧的待办是什么），然后等洪老师说正事。不要急着改代码。

---

## 〇、洪老师是谁（决定你怎么说话）

- 洪晓涛，60 岁 ICU 医生，**编程初学者**，手机 **vivo S18** + iPad。
- **全部用大白话**，不许甩术语；说技术词必须跟一句人话解释。
- 回复别太长，重点放前面，能列表不分段。

## 一、铁规矩（洪老师定的，违反他会生气）

1. **先讨论再做**：新功能先摆方案，他说"确定"才动手。修 bug 可以直接修，但也要先说清病根。
2. 每次改代码：版本号递增 + 三处版本记录（①文件头部"修改记录"注释 ②关于页版本文字 ③代码里 SW_VERSION 常量）。
3. **踩坑必须记录**：每个坑写进对应文件的修改记录里，格式"现象→根因→修法"。
4. 写完必须跑 playwright 自动化测试，全绿才推送。
5. 推送固定三处：xuexi-backup 仓库 → hongxiaotao177.github.io 网站 → 壳资产 android-shell/app/src/main/assets/www/。
6. 本地工作目录 /mnt/agents/output/；测试脚本放 /mnt/agents/output/.test/tXX.py 编号递增。

## 二、家底（账户、钥匙、地址）

- GitHub 用户：**hongxiaotao177**
- 仓库①：**xuexi-backup**（私有，软件+数据备份+壳工程+本说明书）
- 仓库②：**hongxiaotao177.github.io**（公开网站，五件套的网页版）
- **GitHub Token（fine-grained PAT）**：**（公开版不显示，Token 写在本说明书的私有仓库版里，新对话请让洪老师上传文件版）**
  - 权限：上述两仓库 Contents 读写；**2026-08-30 到期**，到期前必须带洪老师换新（这是头等大事）。
  - 注意：这个 Token **没有 Actions 权限**，查不了云端打包状态（403），壳 APK 一律本地打包（见第八节）。
- Kimi API：`POST https://api.moonshot.cn/v1/chat/completions`；key 存在洪老师各设备浏览器的 localStorage（公共钱罐 hx_apikey），**不要写进任何文件**。

## 三、五件套 + 一个壳（当前版本，2026-08-08）

| 软件 | 版本 | 本地文件 | 云端文件名 | 版本文件 |
|---|---|---|---|---|
| 主界面 | 1.2.0 | app/index.html | index.html | version-index.json |
| 学习笔记 | 2.7.0 | 学习笔记.html | software-notes.html | version-notes.json |
| 习题学习 | 1.4.10 | 习题学习.html | software-xiti.html | version-xiti.json |
| 文件桥 | 1.1.3 | 文件桥.html | software-bridge.html | version-bridge.json |
| 血气分析 | 0.10.2 | 血气分析.html | software-xueqi.html | version-xueqi.json |
| PPT工坊 | 0.4.0 | PPT工坊.html | software-ppt.html | version-ppt.json |
| 安卓壳 | **1.0.6** | android-shell/ | 仓库里同路径 | — |

版本文件内容格式：`{"version":"x.y.z","dl":"software-xxx.html","note":"一句话"}`

## 四、关键机制（已经做好的，别重新发明）

1. **公共钱罐 hx_***：localStorage 键 hx_gh_user / hx_gh_tok / hx_gh_repo / hx_apikey / hx_aimodel，五件套共用，一处填处处用。习题学习有 hxAiSync 每 2 秒双向同步。
2. **主界面自检**：SUITE 注册表管五个软件；CUR=壳里内装版本（没装过填 "0"）；reallyMissing()——版本不新也要 fetch 敲门，404 照样下载（治"假记忆"：旧版曾把没装的软件记成已装）。
3. **安卓壳 com.hongxt.shell**：WebView 装 filesDir/www 下的 HTML；固定源 https://app.local/（shouldInterceptRequest 递文件，localStorage 才持久）；JS 桥 window.LearnShell：pickFolder / folderSet / folderName / listFiles / readFile(返base64) / writeFile / deleteFile / applyUpdate / toast；SAF 授权文件夹持久记住；v1.0.4 补了 JS 对话框三件套；**v1.0.6 补了 WebView 文件选择器**（网页里选文件按钮点了没反应的病根）。
4. **同步官 syncShared（主界面 v1.2.0）**：坚果云授权文件夹 ↔ 仓库 shared/ 区。规矩：新盖旧、只增不删、PDF 和 >4MB 不进仓库、台账 shared-meta.json、6 小时节流、7 天没同步亮黄条。台账没记录的两边都有→以坚果云为主上传；台账文件自己不参与同步。主界面有「🗄️ 仓库文件柜」= iPad 取件入口。
5. **文献存放死规矩**：PDF 原件的家＝坚果云授权文件夹（书架，本地+云端双备份）；学习笔记文献库只存提取的正文+AI总结（localStorage 'bijiben_lit_v1' 元数据 + IndexedDB 'lit-db'/papers 正文）；「📂 从坚果云书架取文献」按钮只在壳里显示。**软件读的是本地文件夹，上云归坚果云客户端管**。
6. **PPT工坊**：Markdown 规矩 #封面/##分页/-要点/|表格|/!!大数字/>金句/```chart（同行JSON兼容）；六版式；纯SVG图表；双链 [[文件名]] 或 [[文件名|显示字]]（Obsidian 式渲染不露括号）；点链接四路取件（壳readFile→文件桥bridge-db→仓库shared→影子副本）；导出版内嵌影子副本（优先级：项目linkNotes > 文献库AI总结 > 全文前2000字）；涂黑编辑（v0.3.0 浮条四按钮 + v0.4.0 链接某段=AI拿涂黑观点读文献正文摘1~2段+大白话，没正文/没涂黑退老办法）；生成后每页可编辑（手动/AI/插链接）。
7. **学习笔记↔PPT工坊邮路**：学习笔记详情页「📊 生成PPT大纲」→ 写 localStorage 'ppt_inbox' → PPT工坊收件箱收下（同源才收得到）。发送成功提示 z-index 要 99（面板遮罩 80 会盖）。

## 五、推送速查（每次都要做全套）

```python
import json, base64, urllib.request
from urllib.parse import quote
TOKEN='（见第二节）'
def gh_put(repo, path, content_bytes, msg):
    url=f'https://api.github.com/repos/hongxiaotao177/{repo}/contents/{path}'
    H={'Authorization':'token '+TOKEN,'Accept':'application/vnd.github+json'}
    try: sha=json.load(urllib.request.urlopen(urllib.request.Request(url,headers=H))).get('sha')
    except Exception: sha=None
    body={'message':msg,'content':base64.b64encode(content_bytes).decode()}
    if sha: body['sha']=sha
    return urllib.request.urlopen(urllib.request.Request(url,data=json.dumps(body).encode(),headers=H,method='PUT')).status
```

- 改了哪个软件：推它的 software-xxx.html + version-xxx.json（xuexi-backup），同文件推网站仓库（中文文件名 path 要 quote()），再 cp 到壳 assets/www。
- 壳 assets/www 里六个文件也要推仓库（android-shell/app/src/main/assets/www/）。
- IPython 环境经常重置：上面这段 gh_put 每次重新定义。

## 六、测试怪癖（都是真踩过的坑）

1. 同一浏览器第二个 new_page 开 file:// 页面：localStorage 不持久、JS 异常 → 每个测试页开独立 browser。
2. playwright 下载的 dl.path() 临时文件没 .html 后缀，Chromium 不当网页跑 → 用 dl.save_as 加后缀。
3. add_init_script 对第二个页面可能不跑。
4. Python 字符串里 `\n` 会被吃成真换行毁掉 JS → 用 chr(10) 拼接。
5. GitHub contents API 的 URL 里 `/` 会被 encodeURIComponent 成 %2F → mock 路由要 unquote。
6. 页面 reload 会清空 window 上 mock 的变量 → reload 后重新注入。
7. LearnShell.listFiles 查的是授权文件夹，不是壳内部 www → 判壳内文件缺失要 fetch https://app.local/ 敲门。
8. 悬空按钮（hover 才显示的）点不动 → 用 page.evaluate 直接 el.click()。
9. 测试里 mock AI：route '**/api.moonshot.cn/**' 返回 {'choices':[{'message':{'content':'...'}}]}。

## 七、本地打 APK（壳改版时用，云端 Actions 查不了状态就本地打）

1. SDK 在 /tmp/android-sdk（cmdline-tools + platform-34 + build-tools;34.0.0），没有就用 sdkmanager 装。
2. gradle 用腾讯镜像下载快：https://mirrors.cloud.tencent.com/gradle/gradle-8.9-bin.zip （官方源慢到超时）。
3. 打包：`LANG=C.UTF-8 /tmp/gradle-8.9/bin/gradle -p android-shell assembleRelease --no-daemon`（**必须 LANG=C.UTF-8**，否则中文文件名 assets 读不了报错）。
4. 产物 app/build/outputs/apk/release/app-release.apk → 复制成 学习套装.apk 给洪老师。
5. 签名钥匙 debug.keystore（storePassword/keyPassword 都是 hxt123456，alias shell）在工程里，覆盖安装数据不丢。
6. 改壳记得 versionCode+1、versionName 递增。

## 八、待办清单（按要紧程度排）

1. **Token 2026-08-30 到期**：到期前带洪老师 3 分钟换新（github.com → Settings → Developer settings → Fine-grained tokens），换完更新公共钱罐 hx_gh_tok。
2. 使用说明书（一页大白话图文）：一直欠着。
3. 文件桥「自助上架」方案A：讨论过未拍板。
4. 洪老师另外三个小软件打包进壳：等他传文件。
5. SV650 操作档案：等他真机校正。
6. 血气分析记录（ventHistory/验单history）纳入云备份/对账。

## 九、最近十天干了什么（让新对话有连贯感）

- 五件套入编：PPT工坊注册进主界面，三软件下载整套加第 5 步。
- AI key 全家通用：习题学习 key/模型接公共钱罐。
- 根治 PPT 工坊 404 连环坑（toast 遮挡 / CUR 假已装 / 记忆污染三层根因）。
- PPT 双链 + 导出内嵌影子副本（上课发别人也能看）。
- 坚果云↔仓库同步官 + 仓库文件柜（iPad 入口）。
- PPT 涂黑编辑 + AI 按观点读文献摘段。
- 文献存放死规矩 + 坚果云书架取文献按钮。
- 壳 v1.0.6：补 WebView 文件选择器（导入PDF按钮点不动的病根）。

---

> **洪老师用法**：新开对话 → 点 ➕ 上传这份「交接说明.md」→ 发一句"我是洪晓涛，继续学习套装开发，先读我上传的交接说明，读完用三句话告诉我你明白了什么"。
